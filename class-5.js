 const myName = 'sizan'
 const age = 20
 const job = 'student'
 console.log(`name-${myName} age-${age} job-${job}`);

 const productName = "potato";
 const productPrice = 30;
 const quantity = 3;
 const available = true;

 console.log(`product-${productName} price-${productPrice} quantity-${quantity} available-${available}`);