console.log(person['myName'], person['age'], person['job']);

// array always run in 0 and it is called index number
const product = ['potato', 30, true];
console.log(`productName-${product[0]} price-${product[1]}`);